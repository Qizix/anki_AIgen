# model_config.py

# Configuration settings for the LLM model
MODEL_NAME = "phi4"  # Name of the open-source LLM model
API_KEY = "your_api_key_here"  # API key for accessing the model (if required)
MAX_TOKENS = 150  # Maximum number of tokens to generate
TEMPERATURE = 0.7  # Sampling temperature for randomness in generation
LANGUAGE_OPTIONS = ["English", "Spanish", "French", "German"]  # Supported languages